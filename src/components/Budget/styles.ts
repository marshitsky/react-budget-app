import styled from "styled-components";

const BudgetWrapper = styled.div`
  display: flex;
  gap: 20px;
  flex-direction: column;
`;

const StyledBudgetHeader = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
`;

export { BudgetWrapper, StyledBudgetHeader };
